# Example environment variables for local development
# Copy to .env and customize

# JWT secret for signing tokens (change in production)
JWT_SECRET=your_jwt_secret_here

# Development seed passwords (OPTIONAL). Prefer setting these locally in a .env file
# DEV_TRAINER_PASSWORD and DEV_ATHLETE_PASSWORD are used only for local development
# If omitted, the seed will generate secure random passwords at runtime.
DEV_TRAINER_PASSWORD=
DEV_ATHLETE_PASSWORD=

# Knex/SQLite settings: development uses sqlite file in project root
NODE_ENV=development
